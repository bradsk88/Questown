package ca.bradj.questown.mobs.helperchicken;

/**
 * Closed enum of bubble-icon identifiers rendered above the helper chicken.
 *
 * <p>Each beat state maps to one or two of these icons (U2 + U6).
 * Texture file naming uses the lowercase form of the enum name
 * (see {@code assets/questown/textures/bubble/<name>.png}, U2).
 */
public enum ChickenBubbleIcon {
    STICK,
    WAND,
    UNLIT_CAMPFIRE,
    LIT_CAMPFIRE,
    SUNSET,
    MAP,
    DOOR,
    SIGN,
    CHEST,
    WELCOME_MAT,
    PRESSURE_PLATE,
    WORLDLY_SEEDS,
    VILLAGER_UI,
    FLAG_UI
}
