package ca.bradj.questown.world;

/**
 * Known tool transformation actions for QTWorldAccess.
 * Each value maps to a Forge ToolActions constant in MinecraftWorldAccess.
 */
public enum QTToolAction {
    HOE_TILL,
    AXE_STRIP,
    AXE_SCRAPE,
    SHOVEL_FLATTEN
}
